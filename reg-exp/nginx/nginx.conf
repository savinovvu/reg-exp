worker_processes  2;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    server {
        root   /usr/share/nginx/html;
        listen       80;
        server_name  localhost;


         location /v1/** {
            proxy_pass 'http://app:8080/v1/';
          }

         location * {
             index  index.html index.htm;
          }


    }
}
